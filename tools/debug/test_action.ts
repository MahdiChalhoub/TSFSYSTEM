const { getOrgDefaultTheme } = require('./src/app/actions/settings/theme');
const fetch = require('node-fetch');
// Just a quick check to see if the TS file compiles and doesn't blow up next
console.log("TS action file syntax is valid.");
