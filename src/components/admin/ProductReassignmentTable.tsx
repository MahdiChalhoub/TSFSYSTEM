'use client';

/**
 * STUB COMPONENT - ProductReassignmentTable
 * Placeholder for when Inventory module is not installed.
 */
export function ProductReassignmentTable({ categoryId, products = [] }: any) {
    return (
        <div className="text-center py-8 text-gray-400">
            <p>Inventory module required for product reassignment.</p>
        </div>
    );
}
