# =============================================================================
# Dajingo ERP (TSFSYSTEM) — PRODUCTION Environment Variables
# Loaded by Next.js when NODE_ENV=production (during build and runtime)
# =============================================================================
NODE_ENV=production
NEXT_PUBLIC_ROOT_DOMAIN=tsf.ci
DJANGO_URL=http://127.0.0.1:8000
NEXT_PUBLIC_API_URL=/api
